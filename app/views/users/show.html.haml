%div{ :class => "row" }
  %aside{ :class => "row" }
    %section{ :class => "span4" }
      %h1
        = @user.firstname + " "  + @user.lastname 
  %div{ :class => "span8" }
    - if @user.groups.any?
      %h3 #{ @user.groups.count } Kurse
      %table
        %thead
          %tr
            %td Gruppe
            %td Beginn
            %td Ende
            %td Altersgruppe
            %td Kindergarten
            %td Kategorie
            %td Dauer (min)
            %td Häufigkeit
        %tbody
          = render @groups
      = will_paginate @groups
      = link_to "Neue Gruppe anlegen", newgroup_path, :user_id => :id
