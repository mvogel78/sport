%tr
  %td
    = link_to user.firstname + " " + user.lastname, user
  %td= user.email
  - if current_user.admin? && !current_user?(user)
    %td= link_to "delete", user, method: :delete, data: { confirm: "Sicher?"}
